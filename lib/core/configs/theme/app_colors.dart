import 'package:flutter/material.dart';

class AppColors {
  // Grigio del logo Coind
  static const primary = Color(0xFF707070);

  // Rosso dell'accento e del punto
  static const accent = Color(0xFFE31E24);

  // Bianco per lo sfondo
  static const background = Color(0xFFFFFFFF);

  // Una versione più scura del grigio per contrasto
  static const secondBackground = Color(0xFF404040);
}
